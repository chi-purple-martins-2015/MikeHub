<div class="admin-users-index">
  <h2>All Users</h2>
  <div id = "users-list">
  <ul>
    <% @all_users.each do |user| %>
      <li> <%= link_to(user.first_name, user_path(user.id)) %> </li>
    <% end %>
  </ul>
</div>

</div>